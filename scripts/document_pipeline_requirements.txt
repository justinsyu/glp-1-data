# Optional document parsing and infographic authoring dependencies.
# The sweep itself only needs scripts/sweep/requirements.txt.
llama-cloud-services>=0.6.0
llama-index>=0.12.0
python-dotenv>=1.0
PyMuPDF>=1.24
