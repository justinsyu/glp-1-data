Using the glp-1 treatments identified in this attachment and only focusing on those that are not discontinued or don't have any generic versions (i.e., only focus on branded treatments and investigational treatments): use the exact same approach in terms of style/formatting/pages/etc. from /Desktop/retina-data and create a github pages website for glp-1 treatments. Note that you will have to, like in the retina-data website, get the company logos, color palettes, etc. to create company-specific styling effects. Borrow from retina-data as well for any API keys you need (e.g., llamaparse) but do not reference any content in retina-data since that is a separate repo. Delegate to subagents whenever possible to optimize context use, as well as use subagents in parallel for efficiency. Use live browsers to collect data and setup the same type of automations (press releases, publications/congresses) as in retina-data.